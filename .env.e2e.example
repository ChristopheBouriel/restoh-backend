# E2E Testing Environment
# Copy this to .env.e2e and adjust values as needed
# Or use these values directly in CI environment

NODE_ENV=test

# Use a separate database for E2E tests
# Local: mongodb://localhost:27017/restoh_e2e
# CI: mongodb://localhost:27017/restoh_e2e (from service container)
MONGODB_URI=mongodb://localhost:27017/restoh_e2e

# Server
PORT=3001

# JWT - Use test values (don't use production secrets)
JWT_SECRET=e2e-test-secret-min-32-characters-long
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Frontend URL for CORS
CLIENT_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Stripe - Use test keys or mock
STRIPE_SECRET_KEY=sk_test_mock_key_for_e2e
STRIPE_PUBLISHABLE_KEY=pk_test_mock_key_for_e2e

# Brevo - Optional for E2E (emails are not sent in test mode)
# BREVO_API_KEY=

# Cloudinary - Optional for E2E
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
